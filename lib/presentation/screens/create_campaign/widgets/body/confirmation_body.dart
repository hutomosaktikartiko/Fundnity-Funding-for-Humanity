import 'package:flutter/material.dart';

class ConfirmationBody extends StatelessWidget {
  const ConfirmationBody({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}