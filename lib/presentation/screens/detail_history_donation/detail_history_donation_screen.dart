import 'package:flutter/material.dart';

class DetailHitoryDonationScreen extends StatelessWidget {
  const DetailHitoryDonationScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Detail Hitory Donation Screen'),
      ),
    );
  }
}