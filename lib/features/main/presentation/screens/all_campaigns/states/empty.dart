import 'package:flutter/material.dart';

// TODO: [UI] AllCampaigns Empty 
class Empty extends StatelessWidget {
  const Empty({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}